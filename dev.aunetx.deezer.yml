app-id: dev.aunetx.deezer
runtime: org.freedesktop.Platform
runtime-version: '22.08'
sdk: org.freedesktop.Sdk
base: org.electronjs.Electron2.BaseApp
base-version: '22.08'
sdk-extensions:
  - org.freedesktop.Sdk.Extension.node18
command: run.sh
separate-locales: false
tags: ["proprietary"]
finish-args:
  # Basic permissions
  - --share=ipc
  - --socket=x11
  - --socket=pulseaudio
  - --share=network
  # Allow other instances to see lockfiles
  - --env=TMPDIR=/var/tmp
  # DBus permissions
  - --talk-name=org.freedesktop.Notifications
  - --talk-name=org.gnome.SettingsDaemon
  - --talk-name=org.gnome.SessionManager
  - --own-name=org.mpris.MediaPlayer2.chromium.*
  # Fs access: music/downloads to send mp3, pictures to change profile picture
  - --filesystem=xdg-music:ro
  - --filesystem=xdg-pictures:ro
  - --filesystem=xdg-download:ro
modules:
  - name: deezer
    buildsystem: simple
    sources:
      # The application datas
      - type: file
        path: dev.aunetx.deezer.appdata.xml
      # Wrapper to launch the app
      - type: script
        dest-filename: run.sh
        commands:
          - zypak-wrapper /app/main/deezer-desktop "$@"
      - type: archive
        url: https://github.com/aunetx/deezer-linux/releases/download/v5.30.660-1/deezer-desktop-5.30.660-x64.tar.xz
        sha256: 67c38dcbe5c562daa6aa29aba169f3e0ef09c97b0fe643731225f905c0f36640
        only-arches: ["x86_64"]
        dest: archive
      - type: archive
        url: https://github.com/aunetx/deezer-linux/releases/download/v5.30.660-1/deezer-desktop-5.30.660-arm64.tar.xz
        sha256: 2d855d03d7ea2e24fb99037333b11f6d4381a6ec5aa5b3deb0e9a54fc97ce456
        only-arches: ["aarch64"]
        dest: archive
    build-commands:
      - cp -a archive /app/main

      # Install app wrapper
      - install -Dm755 -t /app/bin/ run.sh

      # Install app datas
      - install -Dm644 dev.aunetx.deezer.appdata.xml /app/share/appdata/dev.aunetx.deezer.appdata.xml
      - install -Dm644 /app/main/resources/dev.aunetx.deezer.svg /app/share/icons/hicolor/scalable/apps/dev.aunetx.deezer.svg
      - install -Dm644 /app/main/resources/dev.aunetx.deezer.desktop /app/share/applications/dev.aunetx.deezer.desktop